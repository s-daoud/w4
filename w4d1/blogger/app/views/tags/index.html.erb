<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_url(tag) %>
      <%= link_to "Delete", tag_url(tag), method: :delete, data: {confirm: "Really delete the tag?"} %>
    </li>
  <% end %>
</ul>
